{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "build": "nuxi build",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "mkdir -p build/data/client && cp -r data/clients.json build/data/ && cp -r data/client/*.json build/data/client/"
  },
  "dependencies": {
    "nuxt": "^3.15.4",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.13.1",
    "@pinia/nuxt": "^0.9.0"
  },
  "engines": {
    "node": "^22.13.1"
  }
}
