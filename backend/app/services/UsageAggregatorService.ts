export async function refreshClientUsages(clientId: string) {
     // 1. Lire les événements (messages envoyés, visiteurs, etc.)
     // 2. Compter
     // 3. Lire le plan et les modules pour définir la limite
     // 4. Mettre à jour la table `client_usages`
}
